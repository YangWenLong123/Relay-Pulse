# GPT/Codex 上游专用 HTTP(S) 代理。若本机可直连 ChatGPT，可保持为空。
# CODEX_UPSTREAM_PROXY_URL=http://127.0.0.1:7890

# 建议为本地 GPT/Codex session 配置独立加密密钥，并在安全位置备份。
# ACCOUNT_SESSION_ENCRYPTION_SECRET=replace-with-a-long-random-secret
